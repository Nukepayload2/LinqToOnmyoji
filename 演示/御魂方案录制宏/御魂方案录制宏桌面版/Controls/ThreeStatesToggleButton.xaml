<UserControl x:Class="ThreeStatesToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:御魂方案录制宏桌面版"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentControl x:Name="CtlHeader" Content="示例" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8"/>
        <ToggleButton x:Name="BtnInclude" Grid.Column="1" BorderThickness="0">〇</ToggleButton>
        <ToggleButton x:Name="BtnExclude" Grid.Column="2" BorderThickness="0">×</ToggleButton>
    </Grid>
</UserControl>
